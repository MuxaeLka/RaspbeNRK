name: nrk_manager
description: NRK Manager — менеджер Raspberry Pi та MikroTik через WireGuard VPN
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.0
  shared_preferences: ^2.2.3
  path_provider: ^2.1.3
  webview_flutter: ^4.7.0
  webview_flutter_android: ^3.16.0
  flutter_animate: ^4.5.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
